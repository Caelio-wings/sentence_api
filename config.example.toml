[server]
# 服务监听地址和端口
host = "0.0.0.0"
port = 8000

[database]
# 数据库类型: "sqlite" 或 "postgresql"
type = "sqlite"

[database.sqlite]
path = "sentences.db"

[database.postgresql]
host = "localhost"
port = 5432
database = "sentences"
user = "postgres"
password = ""
