# UI 设计规范

## 设计理念

温暖、克制、有呼吸感。像一本安静摊开的笔记本，不是管理后台。

## 配色

| 角色       | 色值       |
| ---------- | ---------- |
| 背景       | `#faf9f6` 暖白 |
| 卡片       | `#ffffff` 纯白 |
| 主色       | `#6c5ce7` 紫罗兰 |
| 主色淡     | `#e8e5f7` |
| 文字主     | `#1a1a2e` |
| 文字副     | `#6b7280` |
| 边框       | `#e5e7eb` |
| 成功       | `#059669` |
| 危险       | `#dc2626` |
| 高亮       | `#f59e0b` |

## 排版

- 字体：系统字体栈 `system-ui, -apple-system, sans-serif`
- 字号层级：12 / 14 / 16 / 20 / 28 px
- 行高：1.6 正文，1.2 标题

## 圆角 & 阴影

- 卡片圆角：16px
- 按钮/标签圆角：9999px（药丸形）
- 输入框圆角：12px
- 阴影：`0 1px 3px rgba(0,0,0,0.06), 0 1px 2px rgba(0,0,0,0.04)`

## 布局

- 最大内容宽度：1200px
- 卡片内边距：mobile 16px，desktop 24px
- 栅格间隙：16px
- 响应式断点：640px（移动），1024px（桌面）

## 组件规范

### 按钮
- 主按钮：实心主色背景，白色文字
- 次按钮：透明背景，主色边框
- 危险按钮：红色文字+边框
- 所有按钮：药丸形，hover 微上移 1px + 加深阴影

### 输入框 & 文本域
- 浅灰背景 `#f3f4f6`，聚焦时主色边框
- 聚焦过渡 0.2s ease
- 文本域 3 行高，可拖拽缩放

### 表格
- 无边框，斑马纹行（`#faf9f6` 间隔）
- 移动端自动折叠为卡片列表

### 标签 (Tag / Badge)
- 浅色背景（主色淡），药丸形
- 可删除时右侧显示 ✕ 按钮

### 模态框 / 确认弹窗
- 居中卡片，半透明遮罩
- 不使用浏览器原生 `confirm()`

### 移动端适配
- 表格在 <640px 隐藏表头，每行变为独立卡片
- 操作栏使用图标按钮而非文字
- 顶部固定导航栏，含汉堡菜单
- 触摸目标 ≥ 44px

## 动效

- 页面元素入场：fade-up 0.3s ease
- 悬停交互：transform / box-shadow 0.2s
- 加载态：骨架屏或居中旋转加载图标
